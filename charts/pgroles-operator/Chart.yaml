apiVersion: v2
name: pgroles-operator
description: >-
  Declarative PostgreSQL role and privilege management.
  Define roles, memberships, grants, and default privileges in YAML
  and let the operator converge your database to the desired state.
type: application
version: 0.7.3
appVersion: "0.7.3"
icon: https://raw.githubusercontent.com/hardbyte/pgroles/main/docs/public/logo.svg
sources:
  - https://github.com/hardbyte/pgroles
maintainers:
  - name: Brian Thorne
    email: brian@hardbyte.nz
    url: https://hardbyte.nz
annotations:
  artifacthub.io/changes: |
    - kind: fixed
      description: 'Privilege inspection no longer returns PUBLIC/NULL grantee rows, which previously caused wildcard EXECUTE grants on schemas with extension-managed PUBLIC ACLs to re-plan every reconcile.'
    - kind: changed
      description: 'Wildcard inventory and grantability catalog queries are pre-filtered to the wildcard scope schemas, and ACL queries filter to managed grantees in SQL instead of in Rust.'
  artifacthub.io/category: database
  artifacthub.io/operator: "true"
  artifacthub.io/prerelease: "false"
  artifacthub.io/operatorCapabilities: Basic Install
  artifacthub.io/license: MIT
  artifacthub.io/links: |
    - name: Source
      url: https://github.com/hardbyte/pgroles
  artifacthub.io/crds: |
    - kind: PostgresPolicy
      version: v1alpha1
      name: postgrespolicies.pgroles.io
      displayName: Postgres Policy
      description: Declarative PostgreSQL role graph — roles, memberships, grants, and default privileges.
  artifacthub.io/crdsExamples: |
    - apiVersion: pgroles.io/v1alpha1
      kind: PostgresPolicy
      metadata:
        name: example-policy
      spec:
        connection:
          secretRef:
            name: postgres-credentials
        profiles:
          editor:
            grants:
              - object: { type: table, name: "*" }
                privileges: [SELECT, INSERT, UPDATE, DELETE]
              - object: { type: schema }
                privileges: [USAGE]
          viewer:
            grants:
              - object: { type: table, name: "*" }
                privileges: [SELECT]
              - object: { type: schema }
                privileges: [USAGE]
        schemas:
          - name: public
            profiles: [editor, viewer]
