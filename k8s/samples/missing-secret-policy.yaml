---
apiVersion: pgroles.io/v1alpha1
kind: PostgresPolicy
metadata:
  name: missing-secret-policy
  namespace: default
spec:
  connection:
    secretRef:
      name: does-not-exist
  interval: "5m"

  roles:
    - name: missing-secret-user
      login: true
