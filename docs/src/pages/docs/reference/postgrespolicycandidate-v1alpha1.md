---
title: PostgresPolicyCandidate v1alpha1 reference
description: Generated field reference and validation constraints for PostgresPolicyCandidate.
---

Generated by `crdgen --docs-dir`; edit the Rust schema descriptions, not this page.

This reference describes the served `v1alpha1` API. Requiredness is relative to the containing object; fields in an optional object become relevant when that object is supplied. Schema defaults are shown only when the API server supplies them; runtime defaults are described in the workflow guides. Null is accepted only where the schema says nullable. Status is controller-owned except documented decision writes. Standard `apiVersion`, `kind`, and `metadata` follow [Kubernetes object conventions](https://kubernetes.io/docs/concepts/overview/working-with-objects/).

For workflows and runtime reasons, see [operator guidance](/docs/operator), [approval](/docs/operator-plan-approval), [candidates](/docs/operator-candidates), and [ephemeral access](/docs/ephemeral-access).

## Spec

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `spec` | object | required | — | {"required":["content","policyRef"]} | A one-shot, immutable proposal of policy content.  The operator plans a candidate in its parent policy's execution context and publishes a &#96;PostgresPolicyPlan&#96; for review; the active policy keeps enforcing throughout. A candidate never executes SQL in any state, and its spec cannot be edited — revising a proposal means creating a successor that names the earlier one in &#96;spec.replaces&#96;.  See &#96;docs/src/pages/docs/operator-candidates.md&#96; for the behaviour and &#96;docs/design/adr-001-candidate-api.md&#96; for the API mechanics. |
| `spec` | CEL | — | — | {"message":"candidate spec is immutable","rule":"self == oldSelf"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `spec.content` | object | required | — | — | The proposed policy content. |
| `spec.content.default_owner` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Default owner for ALTER DEFAULT PRIVILEGES (e.g. "app_owner"). |
| `spec.content.default_privileges` | array | optional | — | {"maxItems":512} | One-off default privileges. |
| `spec.content.default_privileges[]` | object | — | — | {"required":["grant"]} | Default privilege configuration.  The scope rules are also expressed as CEL so the API server rejects a bad entry at apply time. &#96;resolved_scope&#96; enforces the same rules for the CLI, which has no admission step. |
| `spec.content.default_privileges[]` | CEL | — | — | {"message":"exactly one of &#96;schema&#96; and &#96;scope&#96; must be set","rule":"has(self.schema) != has(self.scope)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `spec.content.default_privileges[].grant` | array | required | — | {"maxItems":64} | Grantee, privileges, and future object kind to reconcile. |
| `spec.content.default_privileges[].grant[]` | object | — | — | {"required":["on_type","privileges"]} | A single default privilege grant entry. |
| `spec.content.default_privileges[].grant[].ensure` | string | optional | — | {"enum":["present","absent"]} | Desired privilege state: present grants it; absent explicitly revokes it. |
| `spec.content.default_privileges[].grant[].on_type` | string | required | — | {"enum":["table","view","materialized_view","sequence","function","schema","database","type"]} | Kind of future object affected by the default privilege. |
| `spec.content.default_privileges[].grant[].privileges` | array | required | — | {"maxItems":16,"minItems":1} | PostgreSQL privileges to reconcile on the selected objects. |
| `spec.content.default_privileges[].grant[].privileges[]` | string | — | — | {"enum":["SELECT","INSERT","UPDATE","DELETE","TRUNCATE","REFERENCES","TRIGGER","EXECUTE","USAGE","CREATE","CONNECT","TEMPORARY"]} | PostgreSQL privilege types. |
| `spec.content.default_privileges[].grant[].role` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | The role receiving the default privilege. Only used in top-level default_privileges (in profiles, the role is determined by expansion). The exact-uppercase value &#96;PUBLIC&#96; means the PostgreSQL PUBLIC pseudo-role. |
| `spec.content.default_privileges[].owner` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | The role that owns newly created objects. If omitted, uses manifest's default_owner. |
| `spec.content.default_privileges[].schema` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Schema shorthand, equivalent to &#96;scope: {type: schema, schema: ...}&#96;. Exactly one of &#96;schema&#96; and &#96;scope&#96; must be set. |
| `spec.content.default_privileges[].scope` | object | optional | — | {"nullable":true,"required":["type"]} | Where the defaults apply: one schema, or owner-wide (global). Global scope renders &#96;ALTER DEFAULT PRIVILEGES&#96; without an &#96;IN SCHEMA&#96; clause. |
| `spec.content.default_privileges[].scope` | CEL | — | — | {"message":"&#96;schema&#96; is required when type is &#96;schema&#96; and forbidden when type is &#96;global&#96;","rule":"has(self.schema) == (self.type == 'schema')"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `spec.content.default_privileges[].scope.schema` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Schema name. Required for &#96;type: schema&#96;, forbidden for &#96;type: global&#96;. |
| `spec.content.default_privileges[].scope.type` | string | required | — | {"enum":["global","schema"]} | Global or per-schema scope of the default privilege. |
| `spec.content.grants` | array | optional | — | {"maxItems":4096} | One-off grants. |
| `spec.content.grants[]` | object | — | — | {"required":["object","privileges","role"]} | A concrete grant on a specific object or wildcard. |
| `spec.content.grants[].ensure` | string | optional | — | {"enum":["present","absent"]} | Desired object privilege state: present grants it; absent explicitly revokes it. |
| `spec.content.grants[].object` | object | required | — | {"required":["type"]} | Object kind and target to which the privileges apply. |
| `spec.content.grants[].object` | CEL | — | — | {"message":"database grant targets must set &#96;name&#96;","rule":"self.type != 'database' &#124;&#124; has(self.name)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `spec.content.grants[].object.name` | string | optional | — | {"maxLength":256,"minLength":1,"nullable":true} | Object name, or "*" for all objects. Omit for schema-level grants; required for database grants, where it names the connected database. |
| `spec.content.grants[].object.schema` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Schema name. Required for most object types except database. |
| `spec.content.grants[].object.type` | string | required | — | {"enum":["table","view","materialized_view","sequence","function","schema","database","type"]} | PostgreSQL object kind. |
| `spec.content.grants[].privileges` | array | required | — | {"maxItems":16,"minItems":1} | PostgreSQL privileges to reconcile on the selected objects. |
| `spec.content.grants[].privileges[]` | string | — | — | {"enum":["SELECT","INSERT","UPDATE","DELETE","TRUNCATE","REFERENCES","TRIGGER","EXECUTE","USAGE","CREATE","CONNECT","TEMPORARY"]} | PostgreSQL privilege types. |
| `spec.content.grants[].role` | string | required | — | {"maxLength":63,"minLength":1} | The grantee. The exact-uppercase value &#96;PUBLIC&#96; means the PostgreSQL PUBLIC pseudo-role; any other value is an ordinary role name. |
| `spec.content.memberships` | array | optional | — | {"maxItems":2048} | Membership edges. |
| `spec.content.memberships[]` | object | — | — | {"required":["members","role"]} | A membership declaration — which members belong to a role. |
| `spec.content.memberships[].exclusive` | boolean | optional | — | — | Assert that &#96;members&#96; is the complete membership of &#96;role&#96;: plan a REVOKE for any live member not listed here. Only meaningful for predefined (&#96;pg_*&#96;) and &#96;external: true&#96; roles, whose undeclared members are otherwise left untouched — memberships of ordinary managed roles are already reconciled exhaustively. Defaults to &#96;false&#96; so that adopting pgroles never strips provider-granted memberships (for example &#96;pg_monitor&#96; grants made by a cloud platform) without an explicit assertion. |
| `spec.content.memberships[].members` | array | required | — | {"maxItems":512} | Roles that should receive this membership. |
| `spec.content.memberships[].members[]` | object | — | — | {"required":["name"]} | A single member of a role.  Both &#96;inherit&#96; and &#96;admin&#96; are optional. When omitted, they default to &#96;inherit: true&#96; and &#96;admin: false&#96; at resolution time (in &#96;RoleGraph&#96; construction). Keeping them optional in the CRD avoids Kubernetes injecting default values into the stored resource, which causes perpetual diffs in GitOps tools like ArgoCD. |
| `spec.content.memberships[].members[].admin` | boolean | optional | — | {"nullable":true} | Whether the member can administer the role. Defaults to &#96;false&#96;. |
| `spec.content.memberships[].members[].inherit` | boolean | optional | — | {"nullable":true} | Whether the member inherits the role's privileges. Defaults to &#96;true&#96;. |
| `spec.content.memberships[].members[].name` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL role receiving the membership. |
| `spec.content.memberships[].role` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL role granted to the listed members. |
| `spec.content.profiles` | object | optional | — | {"maxProperties":128} | Reusable privilege profiles.  A &#96;BTreeMap&#96; rather than the policy's &#96;HashMap&#96;: the content digest is computed over a canonical serialization, and deterministic iteration order is one less thing that has to be normalised later. |
| `spec.content.profiles.&lt;name&gt;` | object | — | — | — | A reusable privilege profile (CRD-compatible version).  This mirrors &#96;pgroles_core::manifest::Profile&#96; but derives &#96;JsonSchema&#96;. |
| `spec.content.profiles.&lt;name&gt;.config` | object | optional | — | {"maxProperties":32} | Role-level configuration parameter defaults for generated roles, applied via &#96;ALTER ROLE ... SET parameter = value&#96;. Values support the &#96;{schema}&#96; and &#96;{profile}&#96; placeholders, substituted per &#96;schema x profile&#96; expansion (e.g. &#96;search_path: "{schema}"&#96;). |
| `spec.content.profiles.&lt;name&gt;.config.&lt;name&gt;` | string | — | — | {"maxLength":256} | A role configuration parameter value.  Values are always strings — quote numbers and booleans (e.g. &#96;statement_timeout: "30000"&#96;, &#96;jit: "off"&#96;). The Kubernetes CRD schema types config values as strings, and the CLI enforces the same rule so a manifest means the same thing whether it is applied with &#96;pgroles&#96; or &#96;kubectl&#96;. PostgreSQL coerces the string to the parameter's type. |
| `spec.content.profiles.&lt;name&gt;.default_privileges` | array | optional | — | {"maxItems":32} | Privileges to grant on future objects created by the configured owner. |
| `spec.content.profiles.&lt;name&gt;.default_privileges[]` | object | — | — | {"required":["on_type","privileges"]} | Default privilege grant within a profile. |
| `spec.content.profiles.&lt;name&gt;.default_privileges[].ensure` | string | optional | — | {"enum":["present","absent"]} | Whether the privilege must be present or absent. Matches the top-level &#96;default_privileges&#96; entries, which carry the same field. |
| `spec.content.profiles.&lt;name&gt;.default_privileges[].on_type` | string | required | — | {"enum":["table","view","materialized_view","sequence","function","schema","database","type"]} | Kind of future object affected by the default privilege. |
| `spec.content.profiles.&lt;name&gt;.default_privileges[].privileges` | array | required | — | {"maxItems":16,"minItems":1} | PostgreSQL privileges to reconcile on the selected objects. |
| `spec.content.profiles.&lt;name&gt;.default_privileges[].privileges[]` | string | — | — | {"enum":["SELECT","INSERT","UPDATE","DELETE","TRUNCATE","REFERENCES","TRIGGER","EXECUTE","USAGE","CREATE","CONNECT","TEMPORARY"]} | PostgreSQL privilege types. |
| `spec.content.profiles.&lt;name&gt;.default_privileges[].role` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Grantee role; omitted values use the generated profile role. |
| `spec.content.profiles.&lt;name&gt;.grants` | array | optional | — | {"maxItems":64} | Object privilege templates expanded for each bound schema. |
| `spec.content.profiles.&lt;name&gt;.grants[]` | object | — | — | {"required":["object","privileges"]} | Grant template within a profile. |
| `spec.content.profiles.&lt;name&gt;.grants[].ensure` | string | optional | — | {"enum":["present","absent"]} | Whether the privilege must be present or absent. Matches the top-level &#96;grants&#96; entries, which carry the same field. Profiles are additive templates, so validation rejects &#96;absent&#96;; the schema accepts it so the API server does not prune the value before that check can name it. |
| `spec.content.profiles.&lt;name&gt;.grants[].object` | object | required | — | {"required":["type"]} | Object kind and target to which the privileges apply. |
| `spec.content.profiles.&lt;name&gt;.grants[].object.name` | string | optional | — | {"maxLength":256,"minLength":1,"nullable":true} | Object name; omission selects the object-kind scope supported by the profile. |
| `spec.content.profiles.&lt;name&gt;.grants[].object.type` | string | required | — | {"enum":["table","view","materialized_view","sequence","function","schema","database","type"]} | PostgreSQL object kind. |
| `spec.content.profiles.&lt;name&gt;.grants[].privileges` | array | required | — | {"maxItems":16,"minItems":1} | PostgreSQL privileges to reconcile on the selected objects. |
| `spec.content.profiles.&lt;name&gt;.grants[].privileges[]` | string | — | — | {"enum":["SELECT","INSERT","UPDATE","DELETE","TRUNCATE","REFERENCES","TRIGGER","EXECUTE","USAGE","CREATE","CONNECT","TEMPORARY"]} | PostgreSQL privilege types. |
| `spec.content.profiles.&lt;name&gt;.inherit` | boolean | optional | — | {"nullable":true} | Whether privileges from role memberships are inherited automatically. |
| `spec.content.profiles.&lt;name&gt;.login` | boolean | optional | — | {"nullable":true} | Whether the role may initiate database sessions. |
| `spec.content.reconciliation_mode` | string | optional | — | {"enum":["authoritative","additive","adopt"]} | Convergence strategy: how aggressively to converge the database. |
| `spec.content.retirements` | array | optional | — | {"maxItems":512} | Explicit role-retirement workflows for roles that should be removed. |
| `spec.content.retirements[]` | object | — | — | {"required":["role"]} | Declarative workflow for retiring an existing role. |
| `spec.content.retirements[].drop_owned` | boolean | optional | — | — | Whether to run &#96;DROP OWNED BY&#96; before dropping the role. |
| `spec.content.retirements[].reassign_owned_to` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Optional successor role for &#96;REASSIGN OWNED BY ... TO ...&#96;. |
| `spec.content.retirements[].role` | string | required | — | {"maxLength":63,"minLength":1} | The role to retire and ultimately drop. |
| `spec.content.retirements[].terminate_sessions` | boolean | optional | — | — | Whether to terminate other active sessions for the role before drop. |
| `spec.content.roles` | array | optional | — | {"maxItems":1024} | One-off role definitions. |
| `spec.content.roles[]` | object | — | — | {"required":["name"]} | A concrete role definition (CRD-compatible version). |
| `spec.content.roles[].bypassrls` | boolean | optional | — | {"nullable":true} | Whether the role bypasses row-level security. |
| `spec.content.roles[].comment` | string | optional | — | {"maxLength":256,"nullable":true} | Descriptive PostgreSQL role comment. |
| `spec.content.roles[].config` | object | optional | — | {"maxProperties":32} | Role-level configuration parameter defaults, applied via &#96;ALTER ROLE ... SET parameter = value&#96; (e.g. &#96;role: combined&#96;, &#96;search_path: app&#96;). Settings present on the role in the database but absent here are RESET in authoritative mode. |
| `spec.content.roles[].config.&lt;name&gt;` | string | — | — | {"maxLength":256} | A role configuration parameter value.  Values are always strings — quote numbers and booleans (e.g. &#96;statement_timeout: "30000"&#96;, &#96;jit: "off"&#96;). The Kubernetes CRD schema types config values as strings, and the CLI enforces the same rule so a manifest means the same thing whether it is applied with &#96;pgroles&#96; or &#96;kubectl&#96;. PostgreSQL coerces the string to the parameter's type. |
| `spec.content.roles[].connection_limit` | integer | optional | — | {"format":"int32","nullable":true} | Maximum concurrent connections for the role; -1 means unlimited. |
| `spec.content.roles[].createdb` | boolean | optional | — | {"nullable":true} | Whether the role may create databases. |
| `spec.content.roles[].createrole` | boolean | optional | — | {"nullable":true} | Whether the role may create and administer roles, subject to server-version rules. |
| `spec.content.roles[].external` | boolean | optional | — | — | Treat this role as externally managed. The operator may reference it in grants, ownership, and memberships, but will not create, alter, drop, or password-manage it. Declared membership edges remain managed. |
| `spec.content.roles[].inherit` | boolean | optional | — | {"nullable":true} | Whether privileges from role memberships are inherited automatically. |
| `spec.content.roles[].login` | boolean | optional | — | {"nullable":true} | Whether the role may initiate database sessions. |
| `spec.content.roles[].name` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL role name. |
| `spec.content.roles[].password` | object | optional | — | {"nullable":true} | Password source for this role. Either a reference to an existing Secret or a request for the operator to generate one. |
| `spec.content.roles[].password.generate` | object | optional | — | {"nullable":true} | Generate a random password and store it in a new Kubernetes Secret. Mutually exclusive with &#96;secretRef&#96;. |
| `spec.content.roles[].password.generate.length` | integer | optional | — | {"format":"uint32","minimum":0.0,"nullable":true} | Password length. Defaults to 32. Minimum 16, maximum 128. |
| `spec.content.roles[].password.generate.secretKey` | string | optional | — | {"maxLength":253,"minLength":1,"nullable":true} | Key within the generated Secret. Defaults to &#96;password&#96;. |
| `spec.content.roles[].password.generate.secretName` | string | optional | — | {"maxLength":253,"minLength":1,"nullable":true} | Override the generated Secret name. Defaults to &#96;{policy}-pgr-{role}&#96;. |
| `spec.content.roles[].password.secretKey` | string | optional | — | {"maxLength":253,"minLength":1,"nullable":true} | Key within the referenced Secret. Defaults to the role name. Only used with &#96;secretRef&#96;. |
| `spec.content.roles[].password.secretRef` | object | optional | — | {"nullable":true,"required":["name"]} | Reference to an existing Kubernetes Secret containing the password. Mutually exclusive with &#96;generate&#96;. |
| `spec.content.roles[].password.secretRef.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret. |
| `spec.content.roles[].password_valid_until` | string | optional | — | {"maxLength":64,"nullable":true} | Password expiration timestamp (ISO 8601, e.g. "2025-12-31T00:00:00Z"). |
| `spec.content.roles[].preserve_undeclared_grants` | boolean | optional | — | — | Preserve this role's undeclared in-scope object grants during convergence. Revokes against the role are skipped unless the revoked privileges are explicitly asserted absent (&#96;ensure: absent&#96;). |
| `spec.content.roles[].replication` | boolean | optional | — | {"nullable":true} | Whether the role may initiate replication connections. |
| `spec.content.roles[].superuser` | boolean | optional | — | {"nullable":true} | Whether the role bypasses PostgreSQL permission checks as a superuser. |
| `spec.content.schemas` | array | optional | — | {"maxItems":1024} | Schema bindings that expand profiles into concrete roles/grants. |
| `spec.content.schemas[]` | object | — | — | {"required":["name"]} | A schema binding — associates a schema with one or more profiles.  Field bounds come from [&#96;crate::bounds&#96;]; see that module for why they exist and where else they are enforced. |
| `spec.content.schemas[].name` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL schema name. |
| `spec.content.schemas[].owner` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Override default_owner for this schema's default privileges. |
| `spec.content.schemas[].profiles` | array | optional | — | {"maxItems":64} | Profile names to expand for this schema. |
| `spec.content.schemas[].profiles[]` | string | — | — | {"maxLength":63,"minLength":1} | Constraints on this array item, map value, or conditional schema. |
| `spec.content.schemas[].role_pattern` | string | optional | — | {"maxLength":128,"minLength":1} | Role naming pattern. Supports &#96;{schema}&#96; and &#96;{profile}&#96; placeholders. Defaults to &#96;"{schema}-{profile}"&#96;. |
| `spec.policyRef` | object | required | — | {"required":["name"]} | The &#96;PostgresPolicy&#96; this candidate proposes content for. Resolved in the candidate's own namespace: an owner reference cannot cross namespaces, so neither can this. |
| `spec.policyRef.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the referenced resource in the same namespace. |
| `spec.replaces` | string | optional | — | {"maxLength":253,"minLength":1,"nullable":true} | Name of an earlier candidate this one supersedes.  Supersession is always explicit. The operator never infers it from creator identity, because CI typically files every team's candidates under one service account. |
| `spec.target` | object | optional | — | {"nullable":true,"required":["connectionRef"]} | Preview the content against a different connection than the parent policy's. Credentials, locking and the plan's bound target identity all follow the override, which is why such a plan is a preview and never a migration step. |
| `spec.target.connectionRef` | object | required | — | {"required":["key","secretName"]} | Connection information for the candidate evaluation target. |
| `spec.target.connectionRef.key` | string | required | — | {"maxLength":253,"minLength":1} | Key within the Secret holding the connection URL. |
| `spec.target.connectionRef.secretName` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret in the candidate's namespace. |

## Status (read-only except decisions)

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `status` | object | optional | — | {"nullable":true} | Status of a &#96;PostgresPolicyCandidate&#96;.  &#96;phase&#96; is a printable summary; conditions are the source of truth. |
| `status.conditions` | array | optional | [] | {"maxItems":16} | Controller observations about candidate validity, planning, approval, and promotion. |
| `status.conditions[]` | object | — | — | {"required":["status","type"]} | A condition on the &#96;PostgresPolicy&#96; resource. |
| `status.conditions[].last_transition_time` | string | optional | — | {"nullable":true} | Last time the condition transitioned. |
| `status.conditions[].message` | string | optional | — | {"nullable":true} | Human-readable message. |
| `status.conditions[].reason` | string | optional | — | {"nullable":true} | Human-readable reason for the condition. |
| `status.conditions[].status` | string | required | — | — | Status: "True", "False", or "Unknown". |
| `status.conditions[].type` | string | required | — | — | Type of condition: "Ready", "Reconciling", "Degraded". |
| `status.contentDigest` | string | optional | — | {"maxLength":128,"nullable":true} | Canonical digest of &#96;spec.content&#96;, computed by &#96;pgroles_core::candidate::compute_content_digest&#96;. This is what promotion is verified against. |
| `status.observedGeneration` | integer | optional | — | {"format":"int64","nullable":true} | The &#96;.metadata.generation&#96; that was last observed. A candidate spec is immutable, so this advances at most once. |
| `status.phase` | string | optional | "Pending" | {"enum":["Pending","Planned","Promoted","Superseded","Stale"]} | Current candidate evaluation and promotion phase. |
| `status.planRef` | object | optional | — | {"nullable":true,"required":["name"]} | The &#96;PostgresPolicyPlan&#96; produced for this candidate. |
| `status.planRef.name` | string | required | — | — | Name of the PostgresPolicyPlan in the same namespace. |

[Download the complete served OpenAPI schema](/crd-reference/postgrespolicycandidate-v1alpha1.json) for structural composition and all Kubernetes extensions.
