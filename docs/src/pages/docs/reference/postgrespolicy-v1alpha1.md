---
title: PostgresPolicy v1alpha1 reference
description: Generated field reference and validation constraints for PostgresPolicy.
---

Generated by `crdgen --docs-dir`; edit the Rust schema descriptions, not this page.

This reference describes the served `v1alpha1` API. Requiredness is relative to the containing object; fields in an optional object become relevant when that object is supplied. Schema defaults are shown only when the API server supplies them; runtime defaults are described in the workflow guides. Null is accepted only where the schema says nullable. Status is controller-owned except documented decision writes. Standard `apiVersion`, `kind`, and `metadata` follow [Kubernetes object conventions](https://kubernetes.io/docs/concepts/overview/working-with-objects/).

For workflows and runtime reasons, see [operator guidance](/docs/operator), [approval](/docs/operator-plan-approval), [candidates](/docs/operator-candidates), and [ephemeral access](/docs/ephemeral-access).

## Spec

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `spec` | object | required | — | {"required":["connection"]} | Spec for a &#96;PostgresPolicy&#96; custom resource.  Defines the desired state of PostgreSQL roles, grants, default privileges, and memberships for a single database connection. |
| `spec.allow_schema_owner_transfers` | boolean | optional | false | — | Permit adopt mode to transfer schema ownership (&#96;ALTER SCHEMA ... OWNER TO ...&#96;) on schemas whose live owner differs. Without this, apply-mode adopt policies fail such plans with an &#96;OwnerTransferBlocked&#96; condition — the operator equivalent of the CLI's &#96;--allow-schema-owner-transfers&#96;. |
| `spec.approval` | string | optional | — | {"enum":["manual","auto"],"nullable":true} | Approval mode for plans generated by this policy.  Set this explicitly. When omitted it is currently inferred from &#96;spec.mode&#96; (&#96;apply&#96; implies &#96;auto&#96;, &#96;observe&#96; implies &#96;manual&#96;), which leaves a policy's execution gate invisible on the object. That inference is deprecated: a policy relying on it reports an &#96;ApprovalUnset&#96; status condition, and a future release will reject a policy that omits this field. |
| `spec.connection` | object | required | — | — | Database connection configuration. |
| `spec.connection.params` | object | optional | — | {"nullable":true} | Structured connection parameters. Each field is either a plain string or a reference to a Secret key. Mutually exclusive with &#96;secretRef&#96;. |
| `spec.connection.params.auth` | object | optional | — | {"nullable":true} | Provider-backed authentication for connections that use short-lived credentials instead of a static PostgreSQL password. |
| `spec.connection.params.auth.impersonateServiceAccount` | string | optional | — | {"nullable":true} | Target Google service account to impersonate before requesting the Cloud SQL login token. Omit to use the pod's bound identity. |
| `spec.connection.params.auth.scope` | string | optional | — | {"nullable":true} | OAuth scope requested for the access token. |
| `spec.connection.params.auth.type` | string | optional | — | {"enum":["gcp_workload_identity"]} | Provider authentication mechanism. |
| `spec.connection.params.auth` | oneOf branch 1 | conditional | — | — | Fields below apply within this branch. |
| `spec.connection.params.auth` | union | — | — | {"required":["type"]} | Constraints on this array item, map value, or conditional schema. |
| `spec.connection.params.dbname` | string | optional | — | {"nullable":true} | Database name as a literal value. |
| `spec.connection.params.dbnameSecret` | object | optional | — | {"nullable":true,"required":["key","name"]} | Database name from a Secret key. |
| `spec.connection.params.dbnameSecret.key` | string | required | — | {"maxLength":253,"minLength":1} | Key within the Secret. |
| `spec.connection.params.dbnameSecret.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret. |
| `spec.connection.params.host` | string | optional | — | {"nullable":true} | PostgreSQL host as a literal value. |
| `spec.connection.params.hostSecret` | object | optional | — | {"nullable":true,"required":["key","name"]} | PostgreSQL host from a Secret key. |
| `spec.connection.params.hostSecret.key` | string | required | — | {"maxLength":253,"minLength":1} | Key within the Secret. |
| `spec.connection.params.hostSecret.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret. |
| `spec.connection.params.password` | string | optional | — | {"nullable":true} | Password as a literal value (not recommended for production). |
| `spec.connection.params.passwordSecret` | object | optional | — | {"nullable":true,"required":["key","name"]} | Password from a Secret key (recommended). |
| `spec.connection.params.passwordSecret.key` | string | required | — | {"maxLength":253,"minLength":1} | Key within the Secret. |
| `spec.connection.params.passwordSecret.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret. |
| `spec.connection.params.port` | integer | optional | — | {"format":"uint16","maximum":65535.0,"minimum":0.0,"nullable":true} | Port as a literal value. Defaults to 5432 if neither port nor portSecret is set. |
| `spec.connection.params.portSecret` | object | optional | — | {"nullable":true,"required":["key","name"]} | Port from a Secret key. |
| `spec.connection.params.portSecret.key` | string | required | — | {"maxLength":253,"minLength":1} | Key within the Secret. |
| `spec.connection.params.portSecret.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret. |
| `spec.connection.params.setRole` | string | optional | — | {"nullable":true,"pattern":"^[A-Za-z_][A-Za-z0-9_$-]*$"} | Run &#96;SET ROLE "&lt;value&gt;"&#96; once on every pooled connection. Useful when the operator authenticates as a low-privilege identity (e.g. a Cloud SQL IAM user) that has been granted membership in a privileged role like &#96;cloudsqlsuperuser&#96; — PostgreSQL does not inherit role *attributes* (&#96;CREATEROLE&#96;, &#96;CREATEDB&#96;, …) through &#96;GRANT … TO …&#96;, so &#96;SET ROLE&#96; is required for the connection to act with the parent role's attributes.  Must be a simple PostgreSQL identifier matching &#96;^[A-Za-z_][A-Za-z0-9_$-]*$&#96;. The pattern intentionally excludes &#96;@&#96; and &#96;.&#96; — &#96;setRole&#96; is for switching to a privileged *group* role (e.g. &#96;cloudsqlsuperuser&#96;), not an IAM-style principal like &#96;pgroles-operator@project.iam&#96;, which has no extra attributes to inherit via &#96;SET ROLE&#96;. |
| `spec.connection.params.sslMode` | string | optional | — | {"nullable":true} | SSL mode as a literal value. |
| `spec.connection.params.sslModeSecret` | object | optional | — | {"nullable":true,"required":["key","name"]} | SSL mode from a Secret key. |
| `spec.connection.params.sslModeSecret.key` | string | required | — | {"maxLength":253,"minLength":1} | Key within the Secret. |
| `spec.connection.params.sslModeSecret.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret. |
| `spec.connection.params.username` | string | optional | — | {"nullable":true} | Username as a literal value. |
| `spec.connection.params.usernameSecret` | object | optional | — | {"nullable":true,"required":["key","name"]} | Username from a Secret key. |
| `spec.connection.params.usernameSecret.key` | string | required | — | {"maxLength":253,"minLength":1} | Key within the Secret. |
| `spec.connection.params.usernameSecret.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret. |
| `spec.connection.requirePhysicalIdentity` | boolean | optional | — | {"nullable":true} | Refuse to plan or execute unless the target's *physical* identity — &#96;pg_control_system().system_identifier&#96; — can be read.  Off by default: every mainstream managed PostgreSQL exposes the identifier, but PostgreSQL-protocol engines that are not PostgreSQL (CockroachDB, Spanner's PostgreSQL interface, Redshift, Aurora DSQL) do not implement it, and those targets run on the logical identity alone. Set it where a real PostgreSQL is expected and losing the strongest half of the target binding should stop reconciliation rather than silently weaken it. |
| `spec.connection.secretKey` | string | optional | — | {"nullable":true} | Key within the Secret to read. Defaults to &#96;DATABASE_URL&#96;. Only used with &#96;secretRef&#96;. |
| `spec.connection.secretRef` | object | optional | — | {"nullable":true,"required":["name"]} | Reference to a Kubernetes Secret containing a connection URL. Mutually exclusive with &#96;params&#96;. |
| `spec.connection.secretRef.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret. |
| `spec.default_owner` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Default owner for ALTER DEFAULT PRIVILEGES (e.g. "app_owner"). |
| `spec.default_privileges` | array | optional | [] | {"maxItems":512} | One-off default privileges. |
| `spec.default_privileges[]` | object | — | — | {"required":["grant"]} | Default privilege configuration.  The scope rules are also expressed as CEL so the API server rejects a bad entry at apply time. &#96;resolved_scope&#96; enforces the same rules for the CLI, which has no admission step. |
| `spec.default_privileges[]` | CEL | — | — | {"message":"exactly one of &#96;schema&#96; and &#96;scope&#96; must be set","rule":"has(self.schema) != has(self.scope)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `spec.default_privileges[].grant` | array | required | — | {"maxItems":64} | Grantee, privileges, and future object kind to reconcile. |
| `spec.default_privileges[].grant[]` | object | — | — | {"required":["on_type","privileges"]} | A single default privilege grant entry. |
| `spec.default_privileges[].grant[].ensure` | string | optional | — | {"enum":["present","absent"]} | Desired privilege state: present grants it; absent explicitly revokes it. |
| `spec.default_privileges[].grant[].on_type` | string | required | — | {"enum":["table","view","materialized_view","sequence","function","schema","database","type"]} | Kind of future object affected by the default privilege. |
| `spec.default_privileges[].grant[].privileges` | array | required | — | {"maxItems":16,"minItems":1} | PostgreSQL privileges to reconcile on the selected objects. |
| `spec.default_privileges[].grant[].privileges[]` | string | — | — | {"enum":["SELECT","INSERT","UPDATE","DELETE","TRUNCATE","REFERENCES","TRIGGER","EXECUTE","USAGE","CREATE","CONNECT","TEMPORARY"]} | PostgreSQL privilege types. |
| `spec.default_privileges[].grant[].role` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | The role receiving the default privilege. Only used in top-level default_privileges (in profiles, the role is determined by expansion). The exact-uppercase value &#96;PUBLIC&#96; means the PostgreSQL PUBLIC pseudo-role. |
| `spec.default_privileges[].owner` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | The role that owns newly created objects. If omitted, uses manifest's default_owner. |
| `spec.default_privileges[].schema` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Schema shorthand, equivalent to &#96;scope: {type: schema, schema: ...}&#96;. Exactly one of &#96;schema&#96; and &#96;scope&#96; must be set. |
| `spec.default_privileges[].scope` | object | optional | — | {"nullable":true,"required":["type"]} | Where the defaults apply: one schema, or owner-wide (global). Global scope renders &#96;ALTER DEFAULT PRIVILEGES&#96; without an &#96;IN SCHEMA&#96; clause. |
| `spec.default_privileges[].scope` | CEL | — | — | {"message":"&#96;schema&#96; is required when type is &#96;schema&#96; and forbidden when type is &#96;global&#96;","rule":"has(self.schema) == (self.type == 'schema')"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `spec.default_privileges[].scope.schema` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Schema name. Required for &#96;type: schema&#96;, forbidden for &#96;type: global&#96;. |
| `spec.default_privileges[].scope.type` | string | required | — | {"enum":["global","schema"]} | Global or per-schema scope of the default privilege. |
| `spec.grants` | array | optional | [] | {"maxItems":4096} | One-off grants. |
| `spec.grants[]` | object | — | — | {"required":["object","privileges","role"]} | A concrete grant on a specific object or wildcard. |
| `spec.grants[].ensure` | string | optional | — | {"enum":["present","absent"]} | Desired object privilege state: present grants it; absent explicitly revokes it. |
| `spec.grants[].object` | object | required | — | {"required":["type"]} | Object kind and target to which the privileges apply. |
| `spec.grants[].object` | CEL | — | — | {"message":"database grant targets must set &#96;name&#96;","rule":"self.type != 'database' &#124;&#124; has(self.name)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `spec.grants[].object.name` | string | optional | — | {"maxLength":256,"minLength":1,"nullable":true} | Object name, or "*" for all objects. Omit for schema-level grants; required for database grants, where it names the connected database. |
| `spec.grants[].object.schema` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Schema name. Required for most object types except database. |
| `spec.grants[].object.type` | string | required | — | {"enum":["table","view","materialized_view","sequence","function","schema","database","type"]} | PostgreSQL object kind. |
| `spec.grants[].privileges` | array | required | — | {"maxItems":16,"minItems":1} | PostgreSQL privileges to reconcile on the selected objects. |
| `spec.grants[].privileges[]` | string | — | — | {"enum":["SELECT","INSERT","UPDATE","DELETE","TRUNCATE","REFERENCES","TRIGGER","EXECUTE","USAGE","CREATE","CONNECT","TEMPORARY"]} | PostgreSQL privilege types. |
| `spec.grants[].role` | string | required | — | {"maxLength":63,"minLength":1} | The grantee. The exact-uppercase value &#96;PUBLIC&#96; means the PostgreSQL PUBLIC pseudo-role; any other value is an ordinary role name. |
| `spec.interval` | string | optional | "5m" | — | Reconciliation interval (e.g. "5m", "1h"). Defaults to "5m". |
| `spec.memberships` | array | optional | [] | {"maxItems":2048} | Membership edges. |
| `spec.memberships[]` | object | — | — | {"required":["members","role"]} | A membership declaration — which members belong to a role. |
| `spec.memberships[].exclusive` | boolean | optional | — | — | Assert that &#96;members&#96; is the complete membership of &#96;role&#96;: plan a REVOKE for any live member not listed here. Only meaningful for predefined (&#96;pg_*&#96;) and &#96;external: true&#96; roles, whose undeclared members are otherwise left untouched — memberships of ordinary managed roles are already reconciled exhaustively. Defaults to &#96;false&#96; so that adopting pgroles never strips provider-granted memberships (for example &#96;pg_monitor&#96; grants made by a cloud platform) without an explicit assertion. |
| `spec.memberships[].members` | array | required | — | {"maxItems":512} | Roles that should receive this membership. |
| `spec.memberships[].members[]` | object | — | — | {"required":["name"]} | A single member of a role.  Both &#96;inherit&#96; and &#96;admin&#96; are optional. When omitted, they default to &#96;inherit: true&#96; and &#96;admin: false&#96; at resolution time (in &#96;RoleGraph&#96; construction). Keeping them optional in the CRD avoids Kubernetes injecting default values into the stored resource, which causes perpetual diffs in GitOps tools like ArgoCD. |
| `spec.memberships[].members[].admin` | boolean | optional | — | {"nullable":true} | Whether the member can administer the role. Defaults to &#96;false&#96;. |
| `spec.memberships[].members[].inherit` | boolean | optional | — | {"nullable":true} | Whether the member inherits the role's privileges. Defaults to &#96;true&#96;. |
| `spec.memberships[].members[].name` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL role receiving the membership. |
| `spec.memberships[].role` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL role granted to the listed members. |
| `spec.mode` | string | optional | "apply" | {"enum":["apply","observe","plan"]} | Reconciliation mode: &#96;apply&#96; executes SQL, &#96;observe&#96; computes drift only. |
| `spec.profiles` | object | optional | {} | {"maxProperties":128} | Reusable privilege profiles. |
| `spec.profiles.&lt;name&gt;` | object | — | — | — | A reusable privilege profile (CRD-compatible version).  This mirrors &#96;pgroles_core::manifest::Profile&#96; but derives &#96;JsonSchema&#96;. |
| `spec.profiles.&lt;name&gt;.config` | object | optional | — | {"maxProperties":32} | Role-level configuration parameter defaults for generated roles, applied via &#96;ALTER ROLE ... SET parameter = value&#96;. Values support the &#96;{schema}&#96; and &#96;{profile}&#96; placeholders, substituted per &#96;schema x profile&#96; expansion (e.g. &#96;search_path: "{schema}"&#96;). |
| `spec.profiles.&lt;name&gt;.config.&lt;name&gt;` | string | — | — | {"maxLength":256} | A role configuration parameter value.  Values are always strings — quote numbers and booleans (e.g. &#96;statement_timeout: "30000"&#96;, &#96;jit: "off"&#96;). The Kubernetes CRD schema types config values as strings, and the CLI enforces the same rule so a manifest means the same thing whether it is applied with &#96;pgroles&#96; or &#96;kubectl&#96;. PostgreSQL coerces the string to the parameter's type. |
| `spec.profiles.&lt;name&gt;.default_privileges` | array | optional | [] | {"maxItems":32} | Privileges to grant on future objects created by the configured owner. |
| `spec.profiles.&lt;name&gt;.default_privileges[]` | object | — | — | {"required":["on_type","privileges"]} | Default privilege grant within a profile. |
| `spec.profiles.&lt;name&gt;.default_privileges[].ensure` | string | optional | — | {"enum":["present","absent"]} | Whether the privilege must be present or absent. Matches the top-level &#96;default_privileges&#96; entries, which carry the same field. |
| `spec.profiles.&lt;name&gt;.default_privileges[].on_type` | string | required | — | {"enum":["table","view","materialized_view","sequence","function","schema","database","type"]} | Kind of future object affected by the default privilege. |
| `spec.profiles.&lt;name&gt;.default_privileges[].privileges` | array | required | — | {"maxItems":16,"minItems":1} | PostgreSQL privileges to reconcile on the selected objects. |
| `spec.profiles.&lt;name&gt;.default_privileges[].privileges[]` | string | — | — | {"enum":["SELECT","INSERT","UPDATE","DELETE","TRUNCATE","REFERENCES","TRIGGER","EXECUTE","USAGE","CREATE","CONNECT","TEMPORARY"]} | PostgreSQL privilege types. |
| `spec.profiles.&lt;name&gt;.default_privileges[].role` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Grantee role; omitted values use the generated profile role. |
| `spec.profiles.&lt;name&gt;.grants` | array | optional | [] | {"maxItems":64} | Object privilege templates expanded for each bound schema. |
| `spec.profiles.&lt;name&gt;.grants[]` | object | — | — | {"required":["object","privileges"]} | Grant template within a profile. |
| `spec.profiles.&lt;name&gt;.grants[].ensure` | string | optional | — | {"enum":["present","absent"]} | Whether the privilege must be present or absent. Matches the top-level &#96;grants&#96; entries, which carry the same field. Profiles are additive templates, so validation rejects &#96;absent&#96;; the schema accepts it so the API server does not prune the value before that check can name it. |
| `spec.profiles.&lt;name&gt;.grants[].object` | object | required | — | {"required":["type"]} | Object kind and target to which the privileges apply. |
| `spec.profiles.&lt;name&gt;.grants[].object.name` | string | optional | — | {"maxLength":256,"minLength":1,"nullable":true} | Object name; omission selects the object-kind scope supported by the profile. |
| `spec.profiles.&lt;name&gt;.grants[].object.type` | string | required | — | {"enum":["table","view","materialized_view","sequence","function","schema","database","type"]} | PostgreSQL object kind. |
| `spec.profiles.&lt;name&gt;.grants[].privileges` | array | required | — | {"maxItems":16,"minItems":1} | PostgreSQL privileges to reconcile on the selected objects. |
| `spec.profiles.&lt;name&gt;.grants[].privileges[]` | string | — | — | {"enum":["SELECT","INSERT","UPDATE","DELETE","TRUNCATE","REFERENCES","TRIGGER","EXECUTE","USAGE","CREATE","CONNECT","TEMPORARY"]} | PostgreSQL privilege types. |
| `spec.profiles.&lt;name&gt;.inherit` | boolean | optional | — | {"nullable":true} | Whether privileges from role memberships are inherited automatically. |
| `spec.profiles.&lt;name&gt;.login` | boolean | optional | — | {"nullable":true} | Whether the role may initiate database sessions. |
| `spec.reconciliation_mode` | string | optional | "authoritative" | {"enum":["authoritative","additive","adopt"]} | Convergence strategy: how aggressively to converge the database.  - &#96;authoritative&#96; (default): full convergence — anything not in the   manifest is revoked/dropped. - &#96;additive&#96;: only grant, never revoke — safe for incremental adoption;   &#96;ensure: absent&#96; assertions are ignored with a warning condition. - &#96;adopt&#96;: manage declared roles fully, but never drop undeclared roles. |
| `spec.retirements` | array | optional | [] | {"maxItems":512,"x-kubernetes-list-map-keys":["role"],"x-kubernetes-list-type":"map"} | Explicit role-retirement workflows for roles that should be removed.  Keyed by &#96;role&#96;, which is this list's unique identifier rather than &#96;name&#96;. Retiring one role twice was never meaningful, so the key is unambiguous. |
| `spec.retirements[]` | object | — | — | {"required":["role"]} | Declarative workflow for retiring an existing role. |
| `spec.retirements[].drop_owned` | boolean | optional | false | — | Whether to run &#96;DROP OWNED BY&#96; before dropping the role. |
| `spec.retirements[].reassign_owned_to` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Optional successor role for &#96;REASSIGN OWNED BY ... TO ...&#96;. |
| `spec.retirements[].role` | string | required | — | {"maxLength":63,"minLength":1} | The role to retire and ultimately drop. |
| `spec.retirements[].terminate_sessions` | boolean | optional | false | — | Whether to terminate other active sessions for the role before drop. |
| `spec.roles` | array | optional | [] | {"maxItems":1024,"x-kubernetes-list-map-keys":["name"],"x-kubernetes-list-type":"map"} | One-off role definitions.  Keyed by &#96;name&#96;; see &#96;schemas&#96;. |
| `spec.roles[]` | object | — | — | {"required":["name"]} | A concrete role definition (CRD-compatible version). |
| `spec.roles[].bypassrls` | boolean | optional | — | {"nullable":true} | Whether the role bypasses row-level security. |
| `spec.roles[].comment` | string | optional | — | {"maxLength":256,"nullable":true} | Descriptive PostgreSQL role comment. |
| `spec.roles[].config` | object | optional | — | {"maxProperties":32} | Role-level configuration parameter defaults, applied via &#96;ALTER ROLE ... SET parameter = value&#96; (e.g. &#96;role: combined&#96;, &#96;search_path: app&#96;). Settings present on the role in the database but absent here are RESET in authoritative mode. |
| `spec.roles[].config.&lt;name&gt;` | string | — | — | {"maxLength":256} | A role configuration parameter value.  Values are always strings — quote numbers and booleans (e.g. &#96;statement_timeout: "30000"&#96;, &#96;jit: "off"&#96;). The Kubernetes CRD schema types config values as strings, and the CLI enforces the same rule so a manifest means the same thing whether it is applied with &#96;pgroles&#96; or &#96;kubectl&#96;. PostgreSQL coerces the string to the parameter's type. |
| `spec.roles[].connection_limit` | integer | optional | — | {"format":"int32","nullable":true} | Maximum concurrent connections for the role; -1 means unlimited. |
| `spec.roles[].createdb` | boolean | optional | — | {"nullable":true} | Whether the role may create databases. |
| `spec.roles[].createrole` | boolean | optional | — | {"nullable":true} | Whether the role may create and administer roles, subject to server-version rules. |
| `spec.roles[].external` | boolean | optional | false | — | Treat this role as externally managed. The operator may reference it in grants, ownership, and memberships, but will not create, alter, drop, or password-manage it. Declared membership edges remain managed. |
| `spec.roles[].inherit` | boolean | optional | — | {"nullable":true} | Whether privileges from role memberships are inherited automatically. |
| `spec.roles[].login` | boolean | optional | — | {"nullable":true} | Whether the role may initiate database sessions. |
| `spec.roles[].name` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL role name. |
| `spec.roles[].password` | object | optional | — | {"nullable":true} | Password source for this role. Either a reference to an existing Secret or a request for the operator to generate one. |
| `spec.roles[].password.generate` | object | optional | — | {"nullable":true} | Generate a random password and store it in a new Kubernetes Secret. Mutually exclusive with &#96;secretRef&#96;. |
| `spec.roles[].password.generate.length` | integer | optional | — | {"format":"uint32","minimum":0.0,"nullable":true} | Password length. Defaults to 32. Minimum 16, maximum 128. |
| `spec.roles[].password.generate.secretKey` | string | optional | — | {"maxLength":253,"minLength":1,"nullable":true} | Key within the generated Secret. Defaults to &#96;password&#96;. |
| `spec.roles[].password.generate.secretName` | string | optional | — | {"maxLength":253,"minLength":1,"nullable":true} | Override the generated Secret name. Defaults to &#96;{policy}-pgr-{role}&#96;. |
| `spec.roles[].password.secretKey` | string | optional | — | {"maxLength":253,"minLength":1,"nullable":true} | Key within the referenced Secret. Defaults to the role name. Only used with &#96;secretRef&#96;. |
| `spec.roles[].password.secretRef` | object | optional | — | {"nullable":true,"required":["name"]} | Reference to an existing Kubernetes Secret containing the password. Mutually exclusive with &#96;generate&#96;. |
| `spec.roles[].password.secretRef.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the Secret. |
| `spec.roles[].password_valid_until` | string | optional | — | {"maxLength":64,"nullable":true} | Password expiration timestamp (ISO 8601, e.g. "2025-12-31T00:00:00Z"). |
| `spec.roles[].preserve_undeclared_grants` | boolean | optional | false | — | Preserve this role's undeclared in-scope object grants during convergence. Revokes against the role are skipped unless the revoked privileges are explicitly asserted absent (&#96;ensure: absent&#96;). |
| `spec.roles[].replication` | boolean | optional | — | {"nullable":true} | Whether the role may initiate replication connections. |
| `spec.roles[].superuser` | boolean | optional | — | {"nullable":true} | Whether the role bypasses PostgreSQL permission checks as a superuser. |
| `spec.schemas` | array | optional | [] | {"maxItems":1024,"x-kubernetes-list-map-keys":["name"],"x-kubernetes-list-type":"map"} | Schema bindings that expand profiles into concrete roles/grants.  Keyed by &#96;name&#96; so server-side apply merges entries per schema instead of replacing the whole list. The API server also rejects duplicate keys. |
| `spec.schemas[]` | object | — | — | {"required":["name"]} | A schema binding — associates a schema with one or more profiles.  Field bounds come from [&#96;crate::bounds&#96;]; see that module for why they exist and where else they are enforced. |
| `spec.schemas[].name` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL schema name. |
| `spec.schemas[].owner` | string | optional | — | {"maxLength":63,"minLength":1,"nullable":true} | Override default_owner for this schema's default privileges. |
| `spec.schemas[].profiles` | array | optional | [] | {"maxItems":64} | Profile names to expand for this schema. |
| `spec.schemas[].profiles[]` | string | — | — | {"maxLength":63,"minLength":1} | Constraints on this array item, map value, or conditional schema. |
| `spec.schemas[].role_pattern` | string | optional | "{schema}-{profile}" | {"maxLength":128,"minLength":1} | Role naming pattern. Supports &#96;{schema}&#96; and &#96;{profile}&#96; placeholders. Defaults to &#96;"{schema}-{profile}"&#96;. |
| `spec.suspend` | boolean | optional | false | — | Suspend reconciliation when true. Defaults to false. |

## Status (read-only except decisions)

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `status` | object | optional | — | {"nullable":true} | Status of a &#96;PostgresPolicy&#96; resource. |
| `status.applied_password_source_versions` | object | optional | {} | — | Last applied password source version for each password-managed role. |
| `status.applied_password_source_versions.&lt;name&gt;` | string | — | — | — | Constraints on this array item, map value, or conditional schema. |
| `status.change_summary` | object | optional | — | {"nullable":true} | Summary of changes applied in the last reconciliation. |
| `status.change_summary.default_privileges_revoked` | integer | optional | 0 | {"format":"int32"} | Number of default privilege revoke steps. |
| `status.change_summary.default_privileges_set` | integer | optional | 0 | {"format":"int32"} | Number of default privilege grant steps. |
| `status.change_summary.grants_added` | integer | optional | 0 | {"format":"int32"} | Number of object privilege grant steps. |
| `status.change_summary.grants_revoked` | integer | optional | 0 | {"format":"int32"} | Number of object privilege revoke steps. |
| `status.change_summary.members_added` | integer | optional | 0 | {"format":"int32"} | Number of membership additions. |
| `status.change_summary.members_removed` | integer | optional | 0 | {"format":"int32"} | Number of membership removals. |
| `status.change_summary.passwords_set` | integer | optional | 0 | {"format":"int32"} | Number of password updates. |
| `status.change_summary.roles_altered` | integer | optional | 0 | {"format":"int32"} | Number of role attribute or configuration changes. |
| `status.change_summary.roles_created` | integer | optional | 0 | {"format":"int32"} | Number of role creations. |
| `status.change_summary.roles_dropped` | integer | optional | 0 | {"format":"int32"} | Number of role drops. |
| `status.change_summary.schema_owners_altered` | integer | optional | 0 | {"format":"int32"} | Number of schema ownership changes. |
| `status.change_summary.schemas_created` | integer | optional | 0 | {"format":"int32"} | Number of schema creations. |
| `status.change_summary.sessions_terminated` | integer | optional | 0 | {"format":"int32"} | Number of session-termination steps. |
| `status.change_summary.total` | integer | optional | 0 | {"format":"int32"} | Number of all planned change steps. |
| `status.conditions` | array | optional | [] | — | Standard Kubernetes conditions. |
| `status.conditions[]` | object | — | — | {"required":["status","type"]} | A condition on the &#96;PostgresPolicy&#96; resource. |
| `status.conditions[].last_transition_time` | string | optional | — | {"nullable":true} | Last time the condition transitioned. |
| `status.conditions[].message` | string | optional | — | {"nullable":true} | Human-readable message. |
| `status.conditions[].reason` | string | optional | — | {"nullable":true} | Human-readable reason for the condition. |
| `status.conditions[].status` | string | required | — | — | Status: "True", "False", or "Unknown". |
| `status.conditions[].type` | string | required | — | — | Type of condition: "Ready", "Reconciling", "Degraded". |
| `status.content_digest` | string | optional | — | {"nullable":true} | Canonical digest of this policy's own content, computed by exactly the same function as a candidate's &#96;status.contentDigest&#96; (note the wire names differ: this status object serialises snake_case, so the field is &#96;status.content_digest&#96; here).  Promotion is recognised by comparing the two. The value is also the operator's memory of what the content was on the previous reconcile, which is how an edited-after-approval promotion is distinguished from a policy that simply has not changed while a candidate is under review. |
| `status.current_plan_ref` | object | optional | — | {"nullable":true,"required":["name"]} | Reference to the current/latest plan for this policy. |
| `status.current_plan_ref.name` | string | required | — | — | Name of the PostgresPolicyPlan in the same namespace. |
| `status.lastHandledReconcileAt` | string | optional | — | {"nullable":true} | Last force-reconcile annotation value handled by the operator. |
| `status.last_attempted_generation` | integer | optional | — | {"format":"int64","nullable":true} | The &#96;.metadata.generation&#96; that was last attempted. |
| `status.last_error` | string | optional | — | {"nullable":true} | Last reconcile error message, if any. |
| `status.last_reconcile_mode` | string | optional | — | {"enum":["apply","observe","plan"],"nullable":true} | Policy reconcile mode.  Deliberately not &#96;PartialEq&#96;: &#96;plan&#96; is a deprecated spelling of &#96;observe&#96; that must behave identically everywhere, and an &#96;==&#96; comparison is exactly the kind of site that forgets one of the two. Compare through [&#96;PolicyMode::never_executes&#96;] and [&#96;PolicyMode::is_deprecated_spelling&#96;], or match exhaustively. |
| `status.last_successful_reconcile_time` | string | optional | — | {"nullable":true} | ISO 8601 timestamp of the last successful reconciliation. |
| `status.managed_database_identity` | string | optional | — | {"nullable":true} | Canonical identity of the managed database target. |
| `status.observed_generation` | integer | optional | — | {"format":"int64","nullable":true} | The &#96;.metadata.generation&#96; that was last successfully reconciled. |
| `status.owned_roles` | array | optional | [] | — | Roles claimed by this policy's declared ownership scope. |
| `status.owned_roles[]` | string | — | — | — | Constraints on this array item, map value, or conditional schema. |
| `status.owned_schemas` | array | optional | [] | — | Schemas claimed by this policy's declared ownership scope. |
| `status.owned_schemas[]` | string | — | — | — | Constraints on this array item, map value, or conditional schema. |
| `status.plan_warnings` | array | optional | [] | — | Advisory warnings from the last reconciliation's computed plan — for example adopt-mode schema ownership transfers or an undeclared &#96;default_owner&#96;. Populated even when the plan applied cleanly. |
| `status.plan_warnings[]` | string | — | — | — | Constraints on this array item, map value, or conditional schema. |
| `status.transient_failure_count` | integer | optional | 0 | {"format":"int32"} | Consecutive transient operational failures used for exponential backoff. |

[Download the complete served OpenAPI schema](/crd-reference/postgrespolicy-v1alpha1.json) for structural composition and all Kubernetes extensions.
