---
title: EphemeralAccessPolicy v1alpha1 reference
description: Generated field reference and validation constraints for EphemeralAccessPolicy.
---

Generated by `crdgen --docs-dir`; edit the Rust schema descriptions, not this page.

This reference describes the served `v1alpha1` API. Requiredness is relative to the containing object; fields in an optional object become relevant when that object is supplied. Schema defaults are shown only when the API server supplies them; runtime defaults are described in the workflow guides. Null is accepted only where the schema says nullable. Status is controller-owned except documented decision writes. Standard `apiVersion`, `kind`, and `metadata` follow [Kubernetes object conventions](https://kubernetes.io/docs/concepts/overview/working-with-objects/).

For workflows and runtime reasons, see [operator guidance](/docs/operator), [approval](/docs/operator-plan-approval), [candidates](/docs/operator-candidates), and [ephemeral access](/docs/ephemeral-access).

## Spec

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `spec` | object | required | — | {"required":["approval","justification","maximumDuration","memberships","postgresPolicyRef"]} | A GitOps-managed bundle of PostgreSQL memberships that may be requested. |
| `spec.approval` | object | required | — | {"required":["mode"]} | Whether activation requires a recorded approval decision. |
| `spec.approval.mode` | string | required | — | {"enum":["Automatic","Required"]} | Automatic activates without a human decision; Required waits for approval. |
| `spec.defaultDuration` | string | optional | — | {"maxLength":64,"nullable":true,"pattern":"^([0-9]+[smh])+$"} | Duration used when a request omits requestedDuration; one of the two must be supplied. |
| `spec.description` | string | optional | — | {"maxLength":2048,"nullable":true} | Optional explanation of the access this policy provides. |
| `spec.displayName` | string | optional | — | {"maxLength":128,"nullable":true} | Optional human-readable policy label. |
| `spec.justification` | object | required | — | {"required":["required"]} | Whether requests must explain why access is needed. |
| `spec.justification.required` | boolean | required | — | — | Whether a request must include a non-empty justification. |
| `spec.maximumDuration` | string | required | — | {"maxLength":64,"pattern":"^([0-9]+[smh])+$"} | Longest permitted access duration, using s, m, and h units. |
| `spec.memberships` | array | required | — | {"maxItems":32,"minItems":1} | Role memberships that this policy permits a subject to request. |
| `spec.memberships[]` | object | — | — | {"required":["inherit","role"]} | Constraints on this array item, map value, or conditional schema. |
| `spec.memberships[].inherit` | boolean | required | — | — | Whether privileges from role memberships are inherited automatically. |
| `spec.memberships[].role` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL role whose membership will be granted to the subject. |
| `spec.pendingRequestTTL` | string | optional | "15m" | {"maxLength":64,"pattern":"^([0-9]+[smh])+$"} | Time allowed for a pending request to receive approval before it expires. |
| `spec.postgresPolicyRef` | object | required | — | {"required":["name"]} | PostgresPolicy in this namespace that manages the target database. |
| `spec.postgresPolicyRef.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the referenced resource in the same namespace. |
| `spec.suspend` | boolean | optional | false | — | Stops admission of new access through this policy while retaining revocation handling. |

## Status (read-only except decisions)

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `status` | object | optional | — | {"nullable":true} | Controller observations for an ephemeral access policy. |
| `status.conditions` | array | optional | [] | {"maxItems":16} | Controller observations about acceptance and readiness. |
| `status.conditions[]` | object | — | — | {"required":["status","type"]} | Constraints on this array item, map value, or conditional schema. |
| `status.conditions[].bundleHash` | string | optional | — | {"maxLength":71,"nullable":true} | Digest of the membership bundle to which this decision applies. |
| `status.conditions[].grantedDuration` | string | optional | — | {"maxLength":64,"nullable":true} | Access duration to which this decision applies. |
| `status.conditions[].lastTransitionTime` | string | optional | — | {"maxLength":64,"nullable":true} | Timestamp of the last condition-state transition. |
| `status.conditions[].message` | string | optional | — | {"maxLength":2048,"nullable":true} | Human-readable explanation of the condition. |
| `status.conditions[].reason` | string | optional | — | {"maxLength":128,"nullable":true} | Machine-readable reason for the condition. |
| `status.conditions[].status` | string | required | — | {"maxLength":16,"minLength":1} | Condition state, conventionally True, False, or Unknown. |
| `status.conditions[].type` | string | required | — | {"maxLength":32,"minLength":1} | Lifecycle or decision condition name. |
| `status.observedGeneration` | integer | optional | — | {"format":"int64","nullable":true} | Policy generation last processed by the controller. |
| `status.resolvedRoles` | array | optional | [] | {"maxItems":32} | Validated PostgreSQL roles available through this policy. |
| `status.resolvedRoles[]` | string | — | — | {"maxLength":63,"minLength":1} | Constraints on this array item, map value, or conditional schema. |

[Download the complete served OpenAPI schema](/crd-reference/ephemeralaccesspolicy-v1alpha1.json) for structural composition and all Kubernetes extensions.
