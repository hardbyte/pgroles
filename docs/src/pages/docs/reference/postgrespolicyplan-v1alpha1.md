---
title: PostgresPolicyPlan v1alpha1 reference
description: Generated field reference and validation constraints for PostgresPolicyPlan.
---

Generated by `crdgen --docs-dir`; edit the Rust schema descriptions, not this page.

This reference describes the served `v1alpha1` API. Requiredness is relative to the containing object; fields in an optional object become relevant when that object is supplied. Schema defaults are shown only when the API server supplies them; runtime defaults are described in the workflow guides. Null is accepted only where the schema says nullable. Status is controller-owned except documented decision writes. Standard `apiVersion`, `kind`, and `metadata` follow [Kubernetes object conventions](https://kubernetes.io/docs/concepts/overview/working-with-objects/).

For workflows and runtime reasons, see [operator guidance](/docs/operator), [approval](/docs/operator-plan-approval), [candidates](/docs/operator-candidates), and [ephemeral access](/docs/ephemeral-access).

## Spec

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `spec` | object | required | — | {"required":["managedDatabaseIdentity","policyGeneration","policyRef","reconciliationMode"]} | Spec for a &#96;PostgresPolicyPlan&#96; custom resource.  Represents a computed reconciliation plan for a &#96;PostgresPolicy&#96;. Plans are created by the operator and may require explicit approval before execution. |
| `spec` | CEL | — | — | {"message":"plan origin is immutable once set","rule":"!has(oldSelf.origin) &#124;&#124; (has(self.origin) &amp;&amp; self.origin == oldSelf.origin)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `spec.managedDatabaseIdentity` | string | required | — | — | Database identity string for disambiguation in multi-db setups. |
| `spec.origin` | object | optional | — | {"nullable":true,"required":["kind","name","uid"]} | Origin of this plan. Omitted for ordinary durable reconciliation plans. |
| `spec.origin.baseContentDigest` | string | optional | — | {"maxLength":128,"nullable":true} | Canonical content digest of the *policy* content this candidate plan was computed against — the applied base. A candidate is a complete desired-state snapshot, so an approval is only meaningful against the base it was reviewed on: identical SQL effects do not prove the snapshot still preserves everything the base has come to manage since. Promotion refuses to adopt a plan whose base pin no longer matches the content the policy carried before the merge, and planning supersedes the plan as soon as the base moves. Absent for non-candidate origins. |
| `spec.origin.contentDigest` | string | optional | — | {"maxLength":128,"nullable":true} | Canonical content digest of the originating candidate, and the encoding it was computed under.  This is what binds the reviewed plan to the content that will later be promoted. It lives on the origin rather than in an annotation because a promotion check that can be edited by anyone holding &#96;patch&#96; is not a binding at all. Both fields are absent for non-candidate origins. |
| `spec.origin.contentDigestEncoding` | string | optional | — | {"maxLength":64,"nullable":true} | Version tag of the encoding &#96;contentDigest&#96; was computed under. Digests from different encodings are never comparable, so promotion recognition only matches digests carrying the same tag. Absent for non-candidate origins. |
| `spec.origin.kind` | string | required | — | {"maxLength":63} | Kind of the resource that requested this plan. |
| `spec.origin.name` | string | required | — | {"maxLength":253} | Name of the originating resource. |
| `spec.origin.policyUid` | string | optional | — | {"maxLength":63,"nullable":true} | UID of the &#96;PostgresPolicy&#96; the candidate proposes content for. The plan's &#96;spec.policyRef&#96; names it; the UID is what survives a delete-and-recreate of the same name. |
| `spec.origin.uid` | string | required | — | {"maxLength":63} | UID binding the plan to this exact originating resource. |
| `spec.ownedRoles` | array | optional | [] | — | Roles that this plan covers. |
| `spec.ownedRoles[]` | string | — | — | — | Constraints on this array item, map value, or conditional schema. |
| `spec.ownedSchemas` | array | optional | [] | — | Schemas that this plan covers. |
| `spec.ownedSchemas[]` | string | — | — | — | Constraints on this array item, map value, or conditional schema. |
| `spec.policyGeneration` | integer | required | — | {"format":"int64"} | The policy's &#96;.metadata.generation&#96; at plan time. |
| `spec.policyRef` | object | required | — | {"required":["name"]} | Reference to the policy that generated this plan. |
| `spec.policyRef.name` | string | required | — | — | Name of the originating PostgresPolicy in the same namespace. |
| `spec.reconciliationMode` | string | required | — | {"enum":["authoritative","additive","adopt"]} | Reconciliation mode used for this plan. |
| `spec.scope` | object | optional | — | {"nullable":true,"required":["bundleHash","kind","operation"]} | Narrow execution scope for a non-durable plan. |
| `spec.scope.bundleHash` | string | required | — | — | Digest binding an ephemeral plan to its resolved membership bundle. |
| `spec.scope.kind` | string | required | — | — | Execution scope identifier for the ephemeral membership bundle. |
| `spec.scope.operation` | string | required | — | {"enum":["Activate","Revoke"]} | Ephemeral bundle operation, when this is an ephemeral plan. |

## Status (read-only except decisions)

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `status` | object | optional | — | {"nullable":true} | Status of a &#96;PostgresPolicyPlan&#96; resource.  The decision rules below are the same grammar &#96;EphemeralAccessRequest&#96; uses: a decision is terminal, &#96;Approved&#96; and &#96;Denied&#96; cannot both be true, and the deciding identity is recorded in the same admitted write. What CEL cannot do is check *who* is writing — see [&#96;DecisionActor&#96;]. |
| `status` | CEL | — | — | {"message":"Approved=True and Denied=True are mutually exclusive","rule":"!(self.conditions.exists(c, c.type == 'Approved' &amp;&amp; c.status == 'True') &amp;&amp; self.conditions.exists(c, c.type == 'Denied' &amp;&amp; c.status == 'True'))"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"plan decisions are terminal","rule":"oldSelf.conditions.filter(c, (c.type == 'Approved' &#124;&#124; c.type == 'Denied') &amp;&amp; c.status == 'True').map(c, c.type) == self.conditions.filter(c, (c.type == 'Approved' &#124;&#124; c.type == 'Denied') &amp;&amp; c.status == 'True').map(c, c.type) &#124;&#124; oldSelf.conditions.filter(c, (c.type == 'Approved' &#124;&#124; c.type == 'Denied') &amp;&amp; c.status == 'True').size() == 0"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"decision identity is write-once","rule":"!has(oldSelf.decidedBy) &#124;&#124; (has(self.decidedBy) &amp;&amp; self.decidedBy == oldSelf.decidedBy)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"a terminal plan decision and decidedBy identity must be recorded together","rule":"self.conditions.exists(c, (c.type == 'Approved' &#124;&#124; c.type == 'Denied') &amp;&amp; c.status == 'True') == has(self.decidedBy)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"changeDigest is write-once","rule":"!has(oldSelf.changeDigest) &#124;&#124; (has(self.changeDigest) &amp;&amp; self.changeDigest == oldSelf.changeDigest)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"changeDigestEncoding is write-once","rule":"!has(oldSelf.changeDigestEncoding) &#124;&#124; (has(self.changeDigestEncoding) &amp;&amp; self.changeDigestEncoding == oldSelf.changeDigestEncoding)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"targetPhysicalIdentity is write-once","rule":"!has(oldSelf.targetPhysicalIdentity) &#124;&#124; (has(self.targetPhysicalIdentity) &amp;&amp; self.targetPhysicalIdentity == oldSelf.targetPhysicalIdentity)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"targetLogicalFingerprint is write-once","rule":"!has(oldSelf.targetLogicalFingerprint) &#124;&#124; (has(self.targetLogicalFingerprint) &amp;&amp; self.targetLogicalFingerprint == oldSelf.targetLogicalFingerprint)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"physicalIdentityAvailable is write-once","rule":"!has(oldSelf.physicalIdentityAvailable) &#124;&#124; (has(self.physicalIdentityAvailable) &amp;&amp; self.physicalIdentityAvailable == oldSelf.physicalIdentityAvailable)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status.appliedAt` | string | optional | — | {"nullable":true} | Timestamp when the plan was applied (if applicable). |
| `status.applyingSince` | string | optional | — | {"nullable":true} | Timestamp when the plan entered Applying phase (for stuck detection). |
| `status.changeDigest` | string | optional | — | {"nullable":true} | Canonical semantic digest of the plan's typed effects, bound to the reconciliation mode and target database identity.  This is the approval identity: a decision approves these effects, and execution proceeds only when the recomputed digest still matches. It is stable across recomputation of unchanged effects — notably for password changes, which bind the password *source* rather than the derived verifier. See &#96;pgroles_core::approval&#96;. |
| `status.changeDigestEncoding` | string | optional | — | {"nullable":true} | Version tag of the encoding &#96;change_digest&#96; was computed under. Digests from different encodings are never comparable. |
| `status.changeSummary` | object | optional | — | {"nullable":true} | Summary of changes in this plan. |
| `status.changeSummary.default_privileges_revoked` | integer | optional | 0 | {"format":"int32"} | Number of default privilege revoke steps. |
| `status.changeSummary.default_privileges_set` | integer | optional | 0 | {"format":"int32"} | Number of default privilege grant steps. |
| `status.changeSummary.grants_added` | integer | optional | 0 | {"format":"int32"} | Number of object privilege grant steps. |
| `status.changeSummary.grants_revoked` | integer | optional | 0 | {"format":"int32"} | Number of object privilege revoke steps. |
| `status.changeSummary.members_added` | integer | optional | 0 | {"format":"int32"} | Number of membership additions. |
| `status.changeSummary.members_removed` | integer | optional | 0 | {"format":"int32"} | Number of membership removals. |
| `status.changeSummary.passwords_set` | integer | optional | 0 | {"format":"int32"} | Number of password updates. |
| `status.changeSummary.roles_altered` | integer | optional | 0 | {"format":"int32"} | Number of role attribute or configuration changes. |
| `status.changeSummary.roles_created` | integer | optional | 0 | {"format":"int32"} | Number of role creations. |
| `status.changeSummary.roles_dropped` | integer | optional | 0 | {"format":"int32"} | Number of role drops. |
| `status.changeSummary.schema_owners_altered` | integer | optional | 0 | {"format":"int32"} | Number of schema ownership changes. |
| `status.changeSummary.schemas_created` | integer | optional | 0 | {"format":"int32"} | Number of schema creations. |
| `status.changeSummary.sessions_terminated` | integer | optional | 0 | {"format":"int32"} | Number of session-termination steps. |
| `status.changeSummary.total` | integer | optional | 0 | {"format":"int32"} | Number of all planned change steps. |
| `status.computedAt` | string | optional | — | {"nullable":true} | Timestamp when the plan was computed. |
| `status.conditions` | array | optional | [] | {"maxItems":16} | Standard conditions: Computed, Applied, and the terminal decision conditions &#96;Approved&#96; / &#96;Denied&#96;. |
| `status.conditions[]` | object | — | — | {"required":["status","type"]} | A condition on the &#96;PostgresPolicy&#96; resource. |
| `status.conditions[].last_transition_time` | string | optional | — | {"nullable":true} | Last time the condition transitioned. |
| `status.conditions[].message` | string | optional | — | {"nullable":true} | Human-readable message. |
| `status.conditions[].reason` | string | optional | — | {"nullable":true} | Human-readable reason for the condition. |
| `status.conditions[].status` | string | required | — | — | Status: "True", "False", or "Unknown". |
| `status.conditions[].type` | string | required | — | — | Type of condition: "Ready", "Reconciling", "Degraded". |
| `status.decidedBy` | object | optional | — | {"nullable":true,"required":["username"]} | Kubernetes identity which approved or denied this plan.  Written in the same status update as the terminal decision, and write-once thereafter. The supplied Kyverno reference policy overwrites it from authenticated admission &#96;userInfo&#96;; without that admission layer it is an assertion by whoever wrote the status, not a verified identity. |
| `status.decidedBy.groups` | array | optional | [] | {"maxItems":64} | Kubernetes groups recorded for this actor. |
| `status.decidedBy.groups[]` | string | — | — | {"maxLength":256,"minLength":1} | Constraints on this array item, map value, or conditional schema. |
| `status.decidedBy.uid` | string | optional | — | {"maxLength":128,"nullable":true} | Optional Kubernetes user UID recorded by admission. |
| `status.decidedBy.username` | string | required | — | {"maxLength":512,"minLength":1} | Kubernetes username asserted for this actor; authenticated only when enforced by admission. |
| `status.failedAt` | string | optional | — | {"nullable":true} | Timestamp when the plan entered Failed phase (for dedup window). |
| `status.lastError` | string | optional | — | {"nullable":true} | Error message if apply failed. |
| `status.phase` | string | optional | "Pending" | {"enum":["Pending","Approved","Applying","Applied","Failed","Superseded","Rejected"]} | Phase: Pending, Approved, Applying, Applied, Failed, Superseded. |
| `status.physicalIdentityAvailable` | boolean | optional | — | {"nullable":true} | Whether the physical identity was readable when this plan was computed.  Recorded explicitly rather than inferred from &#96;target_physical_identity&#96; being set, so that "the identifier could not be read" is distinguishable from "this plan predates the field". The difference matters at execution: a plan that had the identifier and now does not is a downgrade and fails closed. |
| `status.redactedSqlHash` | string | optional | — | {"nullable":true} | SHA-256 hash of the redacted SQL preview bytes. This is for storage integrity only; approval and deduplication use &#96;change_digest&#96;. |
| `status.revalidatedAt` | string | optional | — | {"nullable":true} | When the plan was most recently confirmed current. |
| `status.revalidatedGeneration` | integer | optional | — | {"format":"int64","nullable":true} | The owning object's &#96;.metadata.generation&#96; this plan was most recently confirmed current against — the policy's for an ordinary plan, the candidate's for a candidate-origin plan.  A pending policy plan is revalidated on every reconcile. When the policy changes but the resulting effects do not, the plan — and any decision recorded on it — is retained and this advances to the new generation. A candidate's spec is immutable, so a candidate plan's provenance is stamped once at creation; its ongoing revalidation is the digest deduplication itself. It is provenance, never approval identity: &#96;change_digest&#96; is what a decision binds. |
| `status.sqlHash` | string | optional | — | {"nullable":true} | SHA-256 hash of the planned SQL. Retained as a diagnostic for the preview artifact; it is **not** the approval identity, because rendered SQL embeds a freshly salted SCRAM verifier for every password change. Use &#96;change_digest&#96; for approval and deduplication. |
| `status.sqlInline` | string | optional | — | {"nullable":true} | Inline SQL for small plans (below a size threshold). |
| `status.sqlOriginalBytes` | integer | optional | — | {"format":"int64","nullable":true} | Uncompressed byte length of the redacted SQL preview. |
| `status.sqlRef` | object | optional | — | {"nullable":true,"required":["key","name"]} | Reference to ConfigMap containing the full SQL (for large plans). |
| `status.sqlRef.compression` | string | optional | — | {"enum":["gzip",null],"nullable":true} | Compression used for the referenced SQL content. Missing means older uncompressed ConfigMap data. |
| `status.sqlRef.key` | string | required | — | — | Data key containing SQL in the referenced ConfigMap. |
| `status.sqlRef.name` | string | required | — | — | Name of the ConfigMap containing the rendered SQL. |
| `status.sqlStatements` | integer | optional | — | {"format":"int64","nullable":true} | Number of SQL statements in the plan (after wildcard expansion). May be significantly larger than &#96;changeSummary.total&#96; when wildcard grants expand to many per-object statements. |
| `status.sqlStoredBytes` | integer | optional | — | {"format":"int64","nullable":true} | Stored byte length of the SQL preview after inline/truncation/compression. |
| `status.sqlTruncated` | boolean | optional | false | — | True when the SQL preview was truncated because the full redacted SQL could not be persisted within Kubernetes object limits. |
| `status.targetLogicalFingerprint` | string | optional | — | {"nullable":true} | Fingerprint of the resolved connection endpoint (host, port, database) this plan was computed against. |
| `status.targetPhysicalIdentity` | string | optional | — | {"nullable":true} | &#96;pg_control_system().system_identifier&#96; as read from the target when this plan was computed — the storage lineage the approval is bound to. Absent on engines that do not expose it. |

[Download the complete served OpenAPI schema](/crd-reference/postgrespolicyplan-v1alpha1.json) for structural composition and all Kubernetes extensions.
