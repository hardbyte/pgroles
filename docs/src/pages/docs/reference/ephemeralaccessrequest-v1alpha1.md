---
title: EphemeralAccessRequest v1alpha1 reference
description: Generated field reference and validation constraints for EphemeralAccessRequest.
---

Generated by `crdgen --docs-dir`; edit the Rust schema descriptions, not this page.

This reference describes the served `v1alpha1` API. Requiredness is relative to the containing object; fields in an optional object become relevant when that object is supplied. Schema defaults are shown only when the API server supplies them; runtime defaults are described in the workflow guides. Null is accepted only where the schema says nullable. Status is controller-owned except documented decision writes. Standard `apiVersion`, `kind`, and `metadata` follow [Kubernetes object conventions](https://kubernetes.io/docs/concepts/overview/working-with-objects/).

For workflows and runtime reasons, see [operator guidance](/docs/operator), [approval](/docs/operator-plan-approval), [candidates](/docs/operator-candidates), and [ephemeral access](/docs/ephemeral-access).

## Spec

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `spec` | object | required | — | {"required":["accessPolicyRef","requestedBy","subject"]} | One immutable runtime request for a bounded access bundle. |
| `spec` | CEL | — | — | {"message":"request spec is immutable","rule":"self == oldSelf"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `spec.accessPolicyRef` | object | required | — | {"required":["name"]} | EphemeralAccessPolicy in this namespace that defines the requested bundle. |
| `spec.accessPolicyRef.name` | string | required | — | {"maxLength":253,"minLength":1} | Name of the referenced resource in the same namespace. |
| `spec.justification` | string | optional | — | {"maxLength":2048,"nullable":true} | Reason for requesting access; required when the policy demands it. |
| `spec.requestedBy` | object | required | — | {"required":["username"]} | Kubernetes identity which created the request. The supplied Kyverno reference policy overwrites this from authenticated admission &#96;userInfo&#96;. |
| `spec.requestedBy.groups` | array | optional | [] | {"maxItems":64} | Kubernetes groups recorded for this actor. |
| `spec.requestedBy.groups[]` | string | — | — | {"maxLength":256,"minLength":1} | Constraints on this array item, map value, or conditional schema. |
| `spec.requestedBy.uid` | string | optional | — | {"maxLength":128,"nullable":true} | Optional Kubernetes user UID recorded by admission. |
| `spec.requestedBy.username` | string | required | — | {"maxLength":512,"minLength":1} | Kubernetes username asserted for this actor; authenticated only when enforced by admission. |
| `spec.requestedDuration` | string | optional | — | {"maxLength":64,"nullable":true,"pattern":"^([0-9]+[smh])+$"} | Requested access duration; omission uses the access policy default. |
| `spec.subject` | object | required | — | {"required":["role"]} | PostgreSQL role receiving the temporary memberships. |
| `spec.subject.role` | string | required | — | {"maxLength":63,"minLength":1} | Existing PostgreSQL role receiving temporary access. |

## Status (read-only except decisions)

| Path | Type | Presence | Default | Constraints | Description |
| --- | --- | --- | --- | --- | --- |
| `status` | object | optional | — | {"nullable":true} | Controller lifecycle state and approval decisions for an access request. |
| `status` | CEL | — | — | {"message":"resolvedAccess is write-once","rule":"!has(oldSelf.resolvedAccess) &#124;&#124; (has(self.resolvedAccess) &amp;&amp; self.resolvedAccess == oldSelf.resolvedAccess)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"Approved=True and Denied=True are mutually exclusive","rule":"!(self.conditions.exists(c, c.type == 'Approved' &amp;&amp; c.status == 'True') &amp;&amp; self.conditions.exists(c, c.type == 'Denied' &amp;&amp; c.status == 'True'))"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"approval decisions are terminal","rule":"oldSelf.conditions.filter(c, (c.type == 'Approved' &#124;&#124; c.type == 'Denied') &amp;&amp; c.status == 'True').size() == 0 &#124;&#124; self.conditions.filter(c, (c.type == 'Approved' &#124;&#124; c.type == 'Denied') &amp;&amp; c.status == 'True') == oldSelf.conditions.filter(c, (c.type == 'Approved' &#124;&#124; c.type == 'Denied') &amp;&amp; c.status == 'True')"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"decision identity is write-once","rule":"!has(oldSelf.decidedBy) &#124;&#124; (has(self.decidedBy) &amp;&amp; self.decidedBy == oldSelf.decidedBy)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"a terminal approval decision and decidedBy identity must be recorded together","rule":"self.conditions.exists(c, (c.type == 'Approved' &#124;&#124; c.type == 'Denied') &amp;&amp; c.status == 'True') == has(self.decidedBy)"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status` | CEL | — | — | {"message":"request conditions must use a declared lifecycle or decision type","rule":"self.conditions.all(c, c.type in ['Approved', 'Denied', 'Resolved', 'Ready', 'Applied'])"} | Evaluated with self at this path; oldSelf refers to the previous value on update. |
| `status.activatedAt` | string | optional | — | {"maxLength":64,"nullable":true} | Timestamp at which the requested access became active. |
| `status.approvalExpiresAt` | string | optional | — | {"maxLength":64,"nullable":true} | Timestamp after which a pending approval is no longer actionable. |
| `status.conditions` | array | optional | [] | {"maxItems":8} | Lifecycle observations and terminal Approved or Denied decisions. |
| `status.conditions[]` | object | — | — | {"required":["status","type"]} | Constraints on this array item, map value, or conditional schema. |
| `status.conditions[].bundleHash` | string | optional | — | {"maxLength":71,"nullable":true} | Digest of the membership bundle to which this decision applies. |
| `status.conditions[].grantedDuration` | string | optional | — | {"maxLength":64,"nullable":true} | Access duration to which this decision applies. |
| `status.conditions[].lastTransitionTime` | string | optional | — | {"maxLength":64,"nullable":true} | Timestamp of the last condition-state transition. |
| `status.conditions[].message` | string | optional | — | {"maxLength":2048,"nullable":true} | Human-readable explanation of the condition. |
| `status.conditions[].reason` | string | optional | — | {"maxLength":128,"nullable":true} | Machine-readable reason for the condition. |
| `status.conditions[].status` | string | required | — | {"maxLength":16,"minLength":1} | Condition state, conventionally True, False, or Unknown. |
| `status.conditions[].type` | string | required | — | {"maxLength":32,"minLength":1} | Lifecycle or decision condition name. |
| `status.decidedBy` | object | optional | — | {"nullable":true,"required":["username"]} | Kubernetes identity which approved or denied the request. The supplied Kyverno reference policy overwrites this from authenticated admission &#96;userInfo&#96; in the same status update as the terminal decision. |
| `status.decidedBy.groups` | array | optional | [] | {"maxItems":64} | Kubernetes groups recorded for this actor. |
| `status.decidedBy.groups[]` | string | — | — | {"maxLength":256,"minLength":1} | Constraints on this array item, map value, or conditional schema. |
| `status.decidedBy.uid` | string | optional | — | {"maxLength":128,"nullable":true} | Optional Kubernetes user UID recorded by admission. |
| `status.decidedBy.username` | string | required | — | {"maxLength":512,"minLength":1} | Kubernetes username asserted for this actor; authenticated only when enforced by admission. |
| `status.endedAt` | string | optional | — | {"maxLength":64,"nullable":true} | Timestamp at which the request reached its terminal state. |
| `status.expiresAt` | string | optional | — | {"maxLength":64,"nullable":true} | Timestamp at which active access must be revoked. |
| `status.lastError` | string | optional | — | {"maxLength":4096,"nullable":true} | Most recent controller error for this request. |
| `status.phase` | string | optional | "Pending" | {"enum":["Pending","PendingApproval","Applying","Active","Revoking","Ended","Revoked","Cancelled","Denied","ApprovalExpired","Failed"]} | Current request lifecycle phase. |
| `status.resolvedAccess` | object | optional | — | {"nullable":true,"required":["accessPolicyGeneration","accessPolicyUid","bundleEncoding","bundleHash","grantedDuration","memberships","targetDatabaseFingerprint","targetPolicyGeneration","targetPolicyUid"]} | Write-once snapshot of the approved target, duration, and exact memberships. |
| `status.resolvedAccess.accessPolicyGeneration` | integer | required | — | {"format":"int64"} | Generation of the access policy resolved for this request. |
| `status.resolvedAccess.accessPolicyUid` | string | required | — | {"maxLength":128} | UID of the access policy resolved for this request. |
| `status.resolvedAccess.bundleEncoding` | string | required | — | {"maxLength":128} | Versioned encoding used to compute the canonical bundle digest. |
| `status.resolvedAccess.bundleHash` | string | required | — | {"maxLength":71} | SHA-256 digest of the canonical target and membership bundle. |
| `status.resolvedAccess.grantedDuration` | string | required | — | {"maxLength":64} | Resolved duration for which access is granted. |
| `status.resolvedAccess.memberships` | array | required | — | {"maxItems":32} | Exact membership edges frozen for activation and revocation. |
| `status.resolvedAccess.memberships[]` | object | — | — | {"required":["inherit","member","role"]} | Constraints on this array item, map value, or conditional schema. |
| `status.resolvedAccess.memberships[].inherit` | boolean | required | — | — | Whether privileges from role memberships are inherited automatically. |
| `status.resolvedAccess.memberships[].member` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL role receiving the membership. |
| `status.resolvedAccess.memberships[].role` | string | required | — | {"maxLength":63,"minLength":1} | Granted PostgreSQL role. |
| `status.resolvedAccess.targetDatabaseFingerprint` | string | required | — | {"maxLength":71} | SHA-256 fingerprint of resolved host, port, and database name. It binds activation and revocation to one database without persisting secrets. |
| `status.resolvedAccess.targetPolicyGeneration` | integer | required | — | {"format":"int64"} | Generation of the target PostgresPolicy at resolution. |
| `status.resolvedAccess.targetPolicyUid` | string | required | — | {"maxLength":128} | UID of the PostgresPolicy managing the resolved database. |
| `status.retainedMemberships` | array | optional | [] | {"maxItems":32} | Memberships retained because another active request or the base policy still requires them. |
| `status.retainedMemberships[]` | object | — | — | {"required":["inherit","member","role"]} | Constraints on this array item, map value, or conditional schema. |
| `status.retainedMemberships[].inherit` | boolean | required | — | — | Whether privileges from role memberships are inherited automatically. |
| `status.retainedMemberships[].member` | string | required | — | {"maxLength":63,"minLength":1} | PostgreSQL role receiving the membership. |
| `status.retainedMemberships[].role` | string | required | — | {"maxLength":63,"minLength":1} | Granted PostgreSQL role. |

[Download the complete served OpenAPI schema](/crd-reference/ephemeralaccessrequest-v1alpha1.json) for structural composition and all Kubernetes extensions.
