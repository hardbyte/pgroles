\* RevalidationMode = "generational" — see PlanRevalidation.tla for what each proves.

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    RevalidationMode = "generational"
    \* No drift: this configuration exists to demonstrate one violation, and
    \* drift reaches a second one (see PlanRevalidation_drift.cfg) that would
    \* make which of the two TLC reports first an accident of exploration.
    MaxDrifts = 0
    MaxEdits = 2

INVARIANT
    TypeOK
    SummaryMatchesPlan
    ApprovalSurvivesEffectNeutralEdits
