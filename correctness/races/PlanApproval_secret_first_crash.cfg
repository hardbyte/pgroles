\* Execution's two writes — the Secret and the SQL transaction — with a crash
\* allowed between them, in the shipped order: Secret first.
\*
\* Passes. The crash leaves an inert Secret nobody uses; the password never
\* reached the database, the next reconcile re-plans, and the same Secret is
\* adopted. ApprovalsBounded is deliberately not checked here: a crash costs a
\* re-approval by design, which is the price of not executing unreviewed SQL.

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    SqlHashApproval = FALSE
    LockDuringApply = TRUE
    HasPasswordChange = TRUE
    MaxDrifts = 0
    DeferSecretMaterialization = TRUE
    RecordMaterializedVersion = TRUE
    SecretBeforeSql = TRUE
    MaxCrashes = 1
    MaxApprovals = 3

INVARIANT
    TypeOK
    NoUnreviewedExecution
    ExecutedWhatWasApproved
    NoStaleExecution
    NoSecretBeforeApproval
    PasswordRecoverable

PROPERTY
    EventuallyApplies
