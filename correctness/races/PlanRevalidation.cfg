\* RevalidationMode = "semantic" — see PlanRevalidation.tla for what each proves.

SPECIFICATION Spec

CHECK_DEADLOCK FALSE

CONSTANTS
    RevalidationMode = "semantic"
    \* Database drift is what separates "semantic" from a strategy keyed on
    \* the policy generation; see PlanRevalidation_drift.cfg.
    MaxDrifts = 1
    MaxEdits = 2

INVARIANT
    TypeOK
    SummaryMatchesPlan
    ApprovalSurvivesEffectNeutralEdits
    NoEmptyPendingPlan
